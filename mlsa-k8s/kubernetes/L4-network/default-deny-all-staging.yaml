# L4 - NetworkPolicy: Default Deny All (Staging)
# All ingress and egress traffic is denied by default

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: staging
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
