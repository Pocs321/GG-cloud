# L4 - NetworkPolicy: Default Deny All (Production)
# All ingress and egress traffic is denied by default

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: production
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
